/*
The STL includes the following containers:

deque, list, map, multimap, set, multiset, stack, queue, priority_queue, and vector

The STL containers are implemented as template classes that include member functions specifying what operations can be performed on the tlements stored in the data structure specified by the container.

the member functions common to all containers include: the default constructor, copy constructor, destructor, empty(), max_size(), size(), swap(), operator=, and except in priority_queue, siz overloaded relational
operator functions (operator< etc). 

Moreover, the member functions common to all containers except stack, queue and priority_queue ,include the functions
begin(), end(), rbegin(), rend(), erase(), and clear()
*/